---
name: read-article
description: |
  单篇论文/文章深度阅读。从提取、背景调研、引用链挖掘、宝藏挖掘、方法论精析，
  到文章架构规划、教学式 HTML 撰写、配图添加、三路 Review、博客发布的完整管线。
  直接通过 html-blog 发表，不再产出 org 笔记。
  被 academic-research 调用时使用 collect 模式（仅产出 raw 素材 + synthesis，跳过 HTML/发布）。
  触发词：读这篇论文、帮我看看这篇、总结这篇、read this paper、深读、解读。
metadata:
  default-enabled: true
  replaces: [capture-and-summarize, article-research]
---

# Read Article — 单篇论文深度阅读

将**单篇论文/文章**彻底榨干：提取全文 → 背景调研 → 多维度并行深度分析 → 综合整理 → 文章架构规划 → 教学式 HTML 撰写 → 配图添加 → Review → 博客发布。

> **核心理念**：每一次运行都充分榨取信息。不设文字量上限，宁可详尽不可遗漏。
> **最终交付不是聊天里的阶段性摘要，而是可沉淀、可复用、可公开表达的成果**：
> 1. HTML 深度解读长文（通过 `html-blog` 直接生成）
> 2. 博客发布链接
> 3. 可选邮件通知
>
> 保留的中间产出：raw 素材 + synthesis 综合分析（均为知识库沉淀）。

---

## 模式

| 模式 | 触发 | 产出 |
|------|------|------|
| `full`（默认） | 用户直接调用 | raw 素材 + synthesis + HTML + 博客 + 邮件 |
| `collect` | 被 `academic-research` 调用 | raw 素材 + synthesis（跳过架构规划/HTML/发布） |

---

## 执行规则

1. 读本文后，立即用 `todo_write` 创建全阶段 todo 清单。
2. 必须按 Phase 顺序推进；不得把中间结果当作完成态停下。
3. Phase 1-3 的产物只用于**后续分析和写作**，默认不作为最终交付。
4. 进入 Phase 5（HTML 写作）前，必须完成 Phase 4（文章架构规划）。
5. 进入 Phase 5 前，必须读取 `~/.hanako/skills/html-blog/SKILL.md`，所有 HTML 生成与发布必须遵守其规范。
6. 用户若未明确要求"只做分析"，默认执行到 **HTML + 博客发布准备完成**；若发布属于高影响动作，则在最终 push 前遵守安全确认。
7. 涉及配图时，遵循优先级：**用户截图 > arXiv HTML 原图 > Docling 从 PDF 提取的论文配图（非整页截图）> GitHub repo 图 > 代码绘制（mermaid/jsxgraph）> 网络搜图**。禁止使用 Docling docpage/dcoref 整页抽取。AI 生图完全禁止。
8. 只要进入 full 模式写文章，**重要事实性句子必须显式带引用**。至少覆盖：论文贡献表述、实验数值、数据集/基线/指标、作者或项目方声明、时间线、引用链中的前置工作结论、与他作对比时的事实依据。
9. Phase 6 的 fidelity review 必须**回原文核查**：以论文全文、项目页、官方仓库、补充材料等原始来源为准，不得只凭 synthesis、subagent 摘录或记忆通过审查。

---

## 管线总览

```
输入: URL / PDF 路径 / 论文标题
  │
  ▼
Phase 1 · 提取 ──────────── raw/<slug>/sources/* + figures/*
  │
  ▼ ━━━ ━━━ ━━━ ━━━ 并行 ━━━ ━━━ ━━━ ━━━
  ├── Phase 2a · 背景调研 ── subagents/background.md
  ├── Phase 2b · 引用链挖掘 ── subagents/citation.md
  ├── Phase 2c · 宝藏挖掘 ── subagents/treasure.md
  └── Phase 2d · 方法论精析 ── subagents/methodology.md
  │
  ▼
Phase 3 · 综合整理 ──────── synthesis.md（主 agent 执行）
  │
  ▼
Phase 4 · 文章架构规划 ──── phases/article-structure.md [full only]
  │
  ▼ ━━━ ━━━ ━━━ ━━━ 顺序+并行 ━━━ ━━━ ━━━ ━━━
Phase 5 · 多阶段 HTML 撰写 [full only]
  │
  ├── Phase 5a · 术语表 + 前置知识 ── subagents/terminology.md
  ├── Phase 5b · 问题定义 + 动机 ── subagents/problem-definition.md
  │     （5a 完成后启动，5a 产出作为输入）
  ├── ▼ ━━━ 以下三个并行 ━━━
  ├── Phase 5c · 方法写作 ── subagents/method-writing.md
  ├── Phase 5d · 实验写作 ── subagents/experiment-writing.md
  ├── Phase 5e · 代码分析 ── subagents/code-analysis.md（若有 GitHub 仓库）
  ├── ▲ ━━━ 并行结束 ━━━
  ├── Phase 5f · 配图 ── subagents/image-collection.md
  │     （基于 5c/5d 产出中的配图计划执行）
  └── Phase 5g · 总结 + 收获 ── subagents/conclusion-writing.md
        （5b-5e 全部完成后启动）
  │
  ▼
Phase 6 · 三路并行 Review ── subagents/review-{fidelity,completeness,html-format}.md [full only]
  │
  ▼
Phase 7 · 发布 + 邮件 ───── 博客 + URL 邮件 [full only]
```

**Phase 5 执行顺序**：5a → 5b → (5c ∥ 5d ∥ 5e) → 5f → 5g

每个 Phase 5x subagent 输出 Markdown 片段，主 agent 在 5g 完成后合并为完整 HTML。

---

## 渐进式披露路由

| 场景 | 读取文件 |
|---|---|
| 进入 Phase 2 前 | 不需要读额外文件 |
| 进入 Phase 4 前 | `phases/article-structure.md` |
| 进入 Phase 5 前 | `~/.hanako/skills/html-blog/SKILL.md`（唯一事实来源）+ `phases/html-writing.md` |
| 进入 Phase 5.5 前 | 若有代码仓库，读取 `~/.hanako/skills/github-repo-read/SKILL.md` |
| 进入 Phase 6 前 | `subagents/review-*.md` |

**硬性规则**：进入 Phase 5（HTML 写作）前必须读取 `~/.hanako/skills/html-blog/SKILL.md`，不可跳过。

---

## Phase 1 · 提取

### 1.1 生成 slug

```
"MaskGIT: Masked Generative Image Transformer" → maskgit-2022
"Attention Is All You Need" → attention-2017
```

### 1.2 初始化目录

```bash
SLUG="<slug>"
mkdir -p ~/Org/roam/raw/${SLUG}/{sources,images/${SLUG},figures/${SLUG}}
```

### arXiv 论文读取与配图优先级（全文适用）

**arXiv 论文默认优先使用 HTML 版本**：
1. `https://arxiv.org/html/<arxiv-id>` 优先用于正文结构读取、公式/图表定位、figure URL 提取；
2. arXiv source tarball (`https://arxiv.org/e-print/<arxiv-id>`) 用于 HTML 图像缺失、需要 LaTeX/source 原始图片、或 HTML 版本不可用时；
3. arXiv PDF / Docling 文本提取只作为 HTML 不可用或结构缺失时的 fallback；
4. PDF 高 DPI crop 只在 HTML/source/官方图均拿不到可用图片时兜底。

涉及配图时，默认先找真实图片，优先级为：用户笔记/会话图片 → **arXiv HTML 原图** → GitHub/官方项目页图片 → arXiv source tarball 原始图 → PDF 高 DPI crop → `image-search` 搜到的可靠公开图片 → 代码绘制（mermaid/jsxgraph）→ 不配图。AI 生图在学术论文解读中默认禁用，除非用户明确要求“概念示意图”，且必须标注为 AI 示意图，不得伪装成论文原图或事实证据图。

### 1.3 分配提取 subagent

读取指引：`phases/extraction.md`（含完整步骤和 Docling 命令）。

---

## Phase 2 · 并行深度分析（4 个 subagent）

Phase 1 完成后，**同时派出 4 个 subagent**，每个 subagent 读取对应模板文件后执行：

| Subagent | 读取模板 | 职责 |
|---|---|---|
| 背景调研 | `subagents/background.md` | 领域/作者/社区/影响 |
| 引用链挖掘 | `subagents/citation.md` | top 3-5 前置工作深度调研 |
| 宝藏挖掘 | `subagents/treasure.md` | 超参数/计算/失败/公式 |
| 方法论精析 | `subagents/methodology.md` | 架构/推导/损失函数 |

派发 prompt 模板：
```
任务：执行 Phase 2a（背景调研）。

读取模板：~/.hanako/skills/read-article/subagents/background.md
按模板要求执行。

论文标题：<title>
摘要：<abstract>
Slug：<slug>
```

---

## Phase 3 · 综合整理

主 agent 执行（不分配 subagent）。等待 Phase 2 的 4 个 subagent 全部完成后：

1. 读取 4 份分析结果
2. 去重合并，保留所有细节
3. 解决矛盾：不同 subagent 对同一信息的不同描述，以原文为准
4. 生成 `~/Org/roam/raw/<slug>/synthesis.md`（Markdown 格式，便于 Phase 5 消费）

---

## Phase 4 · 文章架构规划 [full only]

> 进入本阶段前，读取 `phases/article-structure.md`。

在动笔写 HTML 之前，先规划文章的骨架结构。参考 historical-narrative 和 academic-research 的结构规划方法。

**核心原则**：论文深度解读不是逐章节翻译，而是以教学式叙事重新组织信息，让读者建立系统认知。

### 工作流程

1. **分析全部素材**：通读 synthesis.md + 4 份分析结果，列出核心贡献、关键公式、实验亮点、失败案例
2. **设计文章结构**：给出 2-3 种章节组织方案（如"方法驱动型"、"问题驱动型"、"对比驱动型"），推荐其一
3. **用户确认**：给出推荐方案，用户确认后进入 HTML 写作；用户无偏好时直接按推荐执行

### 架构规划输出

```
选定结构：[方案名称]
章节目录：
  Section 1 · [标题] — [核心内容 2-3 句]
  Section 2 · [标题] — ...
  ...
配图计划：
  - Section 2：架构图（来源：论文 Fig.2）
  - Section 4：实验结果对比表
  - ...
字数分配：
  - 引言：≥ 250 字
  - 方法核心：≥ 900 字
  - 实验：≥ 450 字
  - 总量：常规论文 ≥ 2500 字；复杂系统/综述论文可上浮到 ≥ 3500 字
```

---



**引用硬规则**：重要事实性句子后必须附引用，默认使用句级 citation；连续几句共享同一事实源时，可在最后一句合并，但必须保证读者清楚这些句子共同由哪个来源支撑。

论文解读不是论文翻译，而是一份**面向读者的教学式指南**。

**读者假设**：
- 有基础 ML/DL 背景
- 对该细分领域不了解或仅了解皮毛
- 希望建立系统认知

**段落级逻辑模板**：每个章节遵循 **Motivation → Intuition → Mechanism** 三层递进：
1. **Motivation**：为什么需要这个方法？上一代方法留下什么问题？
2. **Intuition**：核心思想是什么？用类比让非专家理解
3. **Mechanism**：技术细节如何实现？公式、架构、训练策略

**禁止的写法**：
- ❌ 平铺直叙"X 提出了 Y 方法，使用了 Z 技术"
- ❌ 连续罗列论文贡献，没有叙事逻辑
- ❌ 直接上公式不解释意图

**推荐的写法**：
- ✅ "反直觉发现：简单 MLP 连接器在很多任务上表现优异，说明连接器复杂度不是瓶颈"
- ✅ "在复杂公式前先用文字描述其意图：这个损失函数的目的是..."
- ✅ 使用教师口吻："我们可以看到..."、"一个自然的疑问是..."

### 5.3 字数下限

字数下限用于防止写成短摘要，不应诱导灌水。单篇论文解读默认采用紧凑而充分的教学式长文标准；只有 survey、系统论文或方法特别复杂时才上浮。

| 部分 | 最低要求 |
|------|---------|
| HTML 正文总量 | **常规论文 ≥ 2500 字；复杂系统/综述论文 ≥ 3500 字** |
| 方法核心章节 | ≥ 900 字 |
| 实验分析章节 | ≥ 450 字 |
| 引言/背景章节 | ≥ 250 字 |
| 方法对比（表格+叙述） | ≥ 250 字 |
| 参考来源 | 覆盖论文 + 核心引用 |

### 5.4 配图要求

**配图优先级**：

| 优先级 | 来源 | 适用场景 |
|--------|------|----------|
| 🥇 首选 | **arXiv HTML / 论文原图** | 架构图、方法示意图、实验结果图。arXiv 论文必须先尝试 HTML figure URL |
| 🥈 次选 | **官方项目页 / GitHub 原图 / arXiv source** | README、docs、assets、source tarball 中的原始图片 |
| 🥉 第三 | **PDF 高 DPI crop** | 仅当 HTML/source/官方图拿不到可用图片时兜底 |
| 第四 | **网络搜图** | 可靠公开来源的补充图，必须本地化并标注来源 |
| 第五 | **代码绘制** | mermaid（架构/流程）jsxgraph（数学/函数），只能作为补充示意图，不得替代可获得的论文原图 |
| ❌ 不用 | AI 生图 | 学术解读场景默认禁用 |

**数量要求**：
- HTML 正文中至少 **3 张**关键图片
- 至少 **1 张**来自代码绘制（mermaid/jsxgraph）
- 每张图片必须标注来源（如"来源：论文 Fig.2"）

**图片 HTML 格式**：
```html
<div class="photo">
  <img src="media/images/<slug>/<filename>" alt="描述" loading="lazy">
  <div class="cap">图 N：图片描述（来源：论文名, Fig.N）</div>
</div>
```

### 5.5 图片理解协议

图片提取完成不等于图片理解完成。论文中的架构图、流程图、曲线图、ablation 图、teaser 图若承担方法解释或实验解释作用，必须显式理解后再写入正文。

**默认流程**：
1. **文本优先**：先读正文中引用该图的段落、figure caption、appendix/supplement 解释。
2. **视觉理解工具**：对本地图片调用视觉理解工具，默认优先使用 MiniMax MCP：`mcp_MiniMax_understand_image`。若环境提供 GLM 视觉 MCP 或其他视觉模型，可作为第二通道交叉验证。
3. **局部放大 / OCR**：图中文字太小、曲线数值不清或模块标签密集时，先 crop/放大局部，再做 OCR 或视觉理解。
4. **一致性校验**：视觉理解结果必须与 caption、正文 method section、实验表格和 appendix 对齐；冲突时以论文正文和 caption 为准。
5. **写作边界**：可以写“图中展示了……”，但不得把视觉模型推测当作论文事实。不确定信息写成“图示暗示/可理解为”，并回到正文确认。

**MiniMax MCP 用法示例**：
```text
工具：mcp_MiniMax_understand_image
image_source: /本地路径/figure.png
prompt: 请分析这张论文图：识别所有模块、箭头方向、输入输出、图中文字；解释它与论文方法的对应关系；列出不确定之处，不要臆测。
```

**必须触发视觉理解的场景**：
- 架构图被用于解释模型 pipeline；
- 图中包含重要模块名、箭头、mask、loss、stage；
- 曲线图/柱状图被用于实验趋势分析；
- teaser 图用于解释任务输入输出；
- caption 信息不足以支撑正文解读。

---

## Phase 5.5 · 代码仓库补读（可选但必须询问） [full only]

当论文包含 GitHub / official code / project repository 链接时，Phase 5 Review 修复后、Phase 6 前或 Phase 5 写作过程中，必须询问用户是否开启 `github-repo-read` 补读代码仓库。

询问模板：

```text
这篇论文有官方代码仓库：<repo-url>。是否继续开启 github-repo-read，对代码实现、仓库图片、配置和关键模块做补读，并把结果补充进 HTML 博客？
```

用户同意后：

1. 读取 `~/.hanako/skills/github-repo-read/SKILL.md`。
2. 按 `inspect` 或 `deep-read` 模式分析仓库。
3. 优先收集：GitHub README/docs/assets/figures/images/static 中的图；代码中的配置、模型定义、训练/推理入口。
4. 在 HTML 博客中补充代码实现/仓库图片/复现要点。

用户拒绝或暂缓时：
- HTML 中不要臆测代码实现细节。

注意：
- 若用户一开始就明确要求"论文 + 代码一起读"，可以直接进入 github-repo-read，无需二次询问。
- collect 模式下此阶段跳过。

---

## Phase 6 · 三路并行 Review [full only]

HTML 写作完成后，**同时派出 3 个 Review subagent**：

| Review Agent | 读取模板 | 职责 |
|---|---|---|
| `review-fidelity` | `subagents/review-fidelity.md` | 保真度审查 |
| `review-completeness` | `subagents/review-completeness.md` | 完整性审查 |
| `review-html-format` | `subagents/review-html-format.md` | HTML 规范审查 |

派发 prompt 模板：
```
任务：执行 <review-fidelity|review-completeness|review-html-format> 角度的质量审查。

论文标题：<title>
Slug：<slug>
原始全文：~/Org/roam/raw/<slug>/sources/<slug>.md
HTML 文件：~/gongshangzheng.github.io/src/pages/<slug>.html

读取模板：~/.hanako/skills/read-article/subagents/<review-name>.md
按模板要求逐项检查并输出报告。
```

主 agent 汇总 3 个 Review 报告，按优先级修复。

---

## Phase 7 · 发布 + 邮件 [full only]

### 7.1 发布

```bash
cd ~/gongshangzheng.github.io
node build.js && git add -A && git commit -m "post: <标题>" && git push
```

验证：`https://gongshangzheng.github.io/<slug>.html`

### 7.2 邮件通知

由 html-blog 统一控制邮件发送。创建文章时使用 `--notify` 参数即可：

```bash
node ~/.hanako/skills/html-blog/capture.js <slug> --notify
```

html-blog 发布流程会自动检查 frontmatter 中的 `notify` 字段并发送通知。
上游 skill **不要**自行调用 send.py。

---

## 质量底线

以下内容不得缺失：

| 必须包含 | 来源 | 最低量 |
|---------|------|--------|
| 核心问题与 Insight 分析 | Phase 2 综合 | ≥ 250 字 |
| 完整方法 pipeline（输入→输出） | Phase 2d | ≥ 900 字 |
| 至少 2 个完整数学公式（非综述类） | Phase 2c | — |
| 至少 1 个 baseline 对比表（含数值） | Phase 2c/2d | — |
| 至少 3 个具体超参数 | Phase 2c | — |
| 计算成本信息 | Phase 2c | — |
| 至少 1 个消融实验发现 | Phase 2c | — |
| 失败案例或局限性 | Phase 2c | — |
| 引用链 top 3 前置工作有补充信息 | Phase 2b | — |
| 关键图片 | Phase 1 + 配图策略 | ≥ 3 张（优先论文原图） |
| 架构/流程图 | 代码绘制 | ≥ 1 张 mermaid/jsxgraph |
| **HTML 正文总量** | — | **常规论文 ≥ 2500 字；复杂系统/综述论文 ≥ 3500 字** |

---

## 非学术内容处理

| 调整 | 说明 |
|------|------|
| Phase 2b 引用链 | 简化为"相关文章"搜索 |
| Phase 2c 宝藏挖掘 | 只提取适用部分（如无超参数则跳过） |
| Phase 2d 方法论 | 只分析核心论点和方法 |
| 质量底线 | 相应降低公式/实验要求 |
| 字数下限 | 降至 ≥ 1800 字 |

---

## 故障处理

### Docling 超时或失败

```bash
# 方案 1：下载 PDF 本地提取
curl -L "<url>" -o /tmp/${SLUG}.pdf
python3 ~/.hanako/skills/docling/scripts/convert.py \
  /tmp/${SLUG}.pdf --format markdown --max-pages 10 \
  --output ~/Org/roam/raw/${SLUG}/sources/${SLUG}.md

# 方案 2：OCR 扫描件
python3 ~/.hanako/skills/docling/scripts/convert.py \
  /tmp/${SLUG}.pdf --ocr tesseract --force-ocr \
  --format markdown --output ~/Org/roam/raw/${SLUG}/sources/${SLUG}.md
```

### 用户只给了标题

用 `web_search`（或 GLM 联网搜索 MCP，参考 web-search skill）搜索标题，找到 arXiv / Semantic Scholar 链接后正常走流程。

**回退方案**（内置/MCP 搜索不可用时，使用统一虚拟环境）：
```bash
~/scripts/py_scripts/.venv/bin/python3 \
  ~/.hanako/skills/web-search/scripts/ddgs_search.py "论文标题" \
  --no-expand --max_results 10 --output search_result.md
```
解析搜索结果中的 arXiv / Semantic Scholar / PDF 链接，找到论文后正常走流程。

### Review 发现大量缺口

派一个补充 subagent，针对缺口从 synthesis.md 和原文中补充。
