#+TITLE: DeepSeek Harness 笔记
#+SLUG: deepseek-harness
#+TYPE: original
#+STATUS: idea
#+PROGRESS: 0
#+TARGET_ALIAS: categories/AI/AI Infra
#+TARGET_SUB_ID: auto
#+PIN: false
#+SOURCE_URL: 
#+TAGS: [DeepSeek, Harness, AI, 工具]
#+CREATED_AT: 2026-08-14T09:20:43
#+UPDATED_AT: 2026-08-14T09:20:43
#+PUBLISHED_AT: 
#+PUBLISHED_FILE: 

* DeepSeek Harness 笔记

** 下载

#+begin_src sh
# npm
npx @deepseek-ai/dsh web
# source
git clone https://github.com/deepseek-ai/deepseek-harness.git
cd deepseek-harness
pnpm install
pnpm run build
pnpm dsh web
#+end_src

默认会在 http://127.0.0.1:3080 启动 Web 界面
** 配置模型

跳过。

** Python SDK

#+begin_src sh
  python -m venv .venv && . .venv/bin/activate
  python -m pip install deepseek-harness-sdk

  export DEEPSEEK_API_KEY=sk-xxxxx
  python examples/jsonrpc-agent/minimal.py \
    --workspace /abs/path/to/workspace \
    --session-root /abs/path/to/sessions \
    --session-id example-001 \
    "检查仓库并修复失败的测试。"

  ## 在代码里直接调用
  from deepseek_harness import DeepSeekHarness

  with DeepSeekHarness(
      provider="deepseek-official",
      model="deepseek-v4-flash",
      cwd="/abs/workspace",
      session_root="/abs/sessions",
      cordis="examples/jsonrpc-agent/minimal.cordis.yml",
  ) as harness:
      result = harness.run("修复测试", session_id="example-001")
  print(result.final_response)
#+end_src

适合把 agent 嵌入自己的脚本/流水线，不依赖系统 Node.js（SDK 内置运行时）。
** 命令
| ~dsh web~                       | 启动 web UI |
| ~dsh --profile headless "任务"~ |             |
** 核心概念

- Profile
- 插件化：所有能力都是插件，通过 cordis.yml 组合。
- Capability Seam: 每项能力都由 Service Definition/ Provider/Consumer 三角色组成。
- 会话持久化：所有发给模型的内容都会记录在 session 日志里面。

* 开发插件

** 插件是什么

插件就是一个导出 apply 函数的 TypeScript 模块，框架加载时调用 apply(context)，你通过 context 注册各种能力。

#+begin_src typescript
  import type { Context } from '@deepseek-ai/cordis'

  export const name = 'hello-plugin'

  export function apply(ctx: Context) {
    console.log('[hello-plugin] plugin loaded!')
  }
#+end_src
** 三种形态

*** 函数形式
#+begin_src ts
  import type { Context } from '@deepseek-ai/cordis'
    
  export const name = 'hello-plugin'

  export function apply(ctx: Context) {
    console.log('[hello-plugin] plugin loaded!')
  }
#+end_src
*** 对象形式
*** 类形式


